<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FluentInfo.Controls.PrettyView.TitleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentInfo.Controls.PrettyView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <FontIcon x:Name="typeIcon" Margin="0,4,12,0" VerticalAlignment="Center"/>
        <TextBlock x:Name="titleText" Grid.Column="1" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="WrapWholeWords" VerticalAlignment="Center"/>
        <controls:WrapPanel x:Name="chipsPanel" Grid.Column="2" Margin="12,6,0,0" VerticalAlignment="Center" VerticalSpacing="2" HorizontalSpacing="2"/>
        <TextBlock x:Name="subtitleText" Grid.Row="1" Grid.ColumnSpan="3" Margin="0,4,0,0" TextWrapping="Wrap" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="SemiBold"/>
    </Grid>
</UserControl>
