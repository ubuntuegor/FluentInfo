<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FluentInfo.Controls.PrettyView.TitleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentInfo.Controls.PrettyView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <FontIcon x:Name="typeIcon" Margin="2,4,0,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="titleText" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="WrapWholeWords" VerticalAlignment="Center"/>
        </StackPanel>
        <TextBlock x:Name="subtitleText" Margin="0,4,0,0" TextWrapping="Wrap" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontWeight="SemiBold"/>
        <controls:WrapPanel x:Name="chipsPanel" Margin="0,8,0,0" VerticalSpacing="2" HorizontalSpacing="2"/>
    </StackPanel>
</UserControl>
